

#include "rational.hpp"
#include "vector2d.hpp"

namespace fun {

///  Return the quadrance of  @a v
template <class Vec>
inline constexpr typename Vec::value_type
cross_ratio(const Vec &a, const Vec &b, const Vec &c, const Vec &d) {
  return rational <
}

};
